import React from 'react';
import StoreList from '../components/StoreList';

const User: React.FC = () => {
  return (
    <div>
      <h2>User Dashboard</h2>
      <StoreList />
    </div>
  );
};

export default User;